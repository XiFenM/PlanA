# 自我介绍
面试官您好，我叫沈殷桀（yīn jié）。
我硕士毕业于复旦大学大数据学院，本科毕业于上海大学数学系。
我目前在一家国产自研AI芯片公司担任AI框架开发工程师。我最大的核心竞争力是：具备在非Nvidia（国产DLC）架构上，进行大模型推理框架的深度适配与极致性能调优的全栈能力。

【三个核心成就】 在现职工作中，我主要负责解决了三个层面的挑战：

第一，推理框架的底层重构。 我主导了开源框架 vLLM 在自研芯片上的落地。我不止是做简单的API对齐，而是深入源码实现了 Duo-Attention、Chunked Prefill 等高级特性，并申请了关于**‘列优先矩阵乘’**的计算优化专利，从架构层面解决了长序列推理的显存瓶颈。

第二，多模态大模型的攻坚落地。 我负责了 Qwen2.5-VL 和 Ernie-4.5-VL 等前沿多模态模型的端到端适配。针对多模态特有的计算瓶颈，我攻克了 Conv3d 和 3D-RoPE 等复杂算子，通过异构流水线并行和算子融合策略，将推理耗时降低了 50%以上，成功支持了视频流输入，解决了工业落地的痛点。

第三，超越竞品的性能调优。 我曾通过算子融合（比如 Matmul-Allreduce）和通信优化，在 Llama-3-70B 等模型的特定推理场景下，实现了性能对齐甚至超越 A100 GPU 的成绩。我的优化成果也通过了上海 AI Lab 和信通院的严苛测评。

【结尾】 总结来说，我是一名既懂底层算子（CUDA/Triton），又懂上层框架（vLLM/DeepSpeed）的复合型工程师。我希望能利用我在国产异构算力上的实战经验，为贵公司的AI基础设施建设贡献力量。”
# 项目讲解介绍
MXU固定为128 * 128
对于小矩阵，我们必须要padding到128，
vreg 也必须要128 padding（32bit）8（sub core） * 128
切分数据横跨两个subcore，以128单元的操作很难进行直接计算。
内存层次
所有的内存搬运都需要显式地由开发者制定，可以异步搬运
# 自定义的算子库

# 算子优化手段

算子融合（可以重构算法逻辑）
pingpong（传输与计算并行，double buffer ，交换vmem块）
软件流水线，超长指令字（指令展开），分为两阶段，序言（连续发送多个matmul指令）和稳态（边获取matmul结果，一边发送后面的matmul）减少了硬件上的控制电路，硬件调度乱序执行，硬件不做管理。隐藏发送push gain。

mask数据控制流

模板化kernel预处理分支，分支判断提到cpu，借助cpu的控制器能力分支预测

跨越for循环进行指令展开重排

## 判断优化空间
## 针对不同shape进行优化kernel
64合并/128转

# flash attention系列优化

# vllm调度机制

## Weight only int4量化和fp8量化，你觉得二者有什么应用上的区别
# 为什么只量化weight，不量化activation

## 动态量化和静态量化有听说过吗？区别是？

## 什么时候适合动态量化，什么时候适合静态量化

