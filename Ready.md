# 自我介绍

面试官您好，我叫沈殷桀。
我硕士毕业于复旦大学大数据学院，本科毕业于上海大学数学系。
我目前担任AI框架开发工程师。我过去的工作主要是在非 NV 架构的自研芯片上，适配并优化 vLLM 和 Pytorch  等主流框架。

第一，对 Transformer 推理全栈的深度掌控。 为了在受限硬件上跑通 Qwen2.5-VL 和 Llama-3，我深入修改了 vLLM 源码。我不仅实现了 Chunked Prefill，还针对硬件访存特性，设计了 “列优先矩阵乘” 和 Duo-Attention 机制（并申请了专利）。这证明我不止是“调包”，我有架构级的改造能力。

第二，底层算子开发与性能压榨能力。 我非常熟悉 GPU 体系结构。在工作中，我需要手写 Kernel 来解决性能瓶颈。例如，针对多模态模型中的 Conv3d 和 3D-RoPE，我通过 流水线并行 和 算子融合，将推理延时降低了 50%。 此外，我也有在 V100 上使用 CUDA 开发 的经验，曾手动实现 Softmax 和 Reduce 算子，通过 Shared Memory 优化和解决 Bank Conflict，实现了比 cuDNN 更高的特定场景性能。

第三，解决复杂系统问题的工程能力。 我主导了 DeepSeek-R1 等超大模型在异构硬件上的分布式推理落地，熟悉 Zero-3 原理及 NCCL（我们自研的 CCL）通信库的优化。我习惯通过 Profiling 工具分析 Compute-bound 和 Memory-bound 瓶颈，并给出针对性解决方案。

 简单来说，我的工作是在一个生态尚未成熟的硬件平台上，通过软件和算法的极致优化，去对齐甚至在部分场景下超越 A100 的性能表现。
# 项目讲解介绍
MXU固定为128 * 128
对于小矩阵，我们必须要padding到128，
vreg 也必须要128 padding（32bit）8（sub core） * 128
切分数据横跨两个subcore，以128单元的操作很难进行直接计算。
内存层次
所有的内存搬运都需要显式地由开发者制定，可以异步搬运
# 自定义的算子库

# 算子优化手段

算子融合（可以重构算法逻辑）
pingpong（传输与计算并行，double buffer ，交换vmem块）
软件流水线，超长指令字（指令展开），分为两阶段，序言（连续发送多个matmul指令）和稳态（边获取matmul结果，一边发送后面的matmul）减少了硬件上的控制电路，硬件调度乱序执行，硬件不做管理。隐藏发送push gain。

mask数据控制流

模板化kernel预处理分支，分支判断提到cpu，借助cpu的控制器能力分支预测

跨越for循环进行指令展开重排

## 判断优化空间
## 针对不同shape进行优化kernel
64合并/128转

# flash attention系列优化

# vllm调度机制

## Weight only int4量化和fp8量化，你觉得二者有什么应用上的区别
# 为什么只量化weight，不量化activation

## 动态量化和静态量化有听说过吗？区别是？

## 什么时候适合动态量化，什么时候适合静态量化

