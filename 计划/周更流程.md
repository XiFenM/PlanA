# 周更流程（已退役）

> **状态**：本入口已于 2026-08-12 退役。保留文件路径，只为让历史周报和旧提交中的链接仍可定位；本文不再是可执行 SOP。

资料研究、增量 refresh、claim 级证据、跨报告去重、候选排序与新报告发布，现由中央
`resource-planning` 统一负责。PlanA 的静态 source/query、模块 adapter、空写入槽和存储位置由
[`../.agent-skills-config/resource-planning.json`](../.agent-skills-config/resource-planning.json) 声明；动态事实只在新 registry 中。

当前边界：

- 运行按需触发，日历只作提醒；没有“每周必须生成一份”的要求。
- 联网、付费、登录、保存、发布、课程编辑、提交和推送均需各自授权。
- refresh 必须逐项记录 `covered`、`no-hit`、`blocked` 或 `skipped`；局部失败不能冒充全局无更新。
- 新报告 create-only；registry 最后发布。历史 W18/W26/W32 保持原字节，不补状态、不生成 cursor、不转成当前 backlog。
- 课程资料只能进入预置 `resource-slot`，且必须经过精确 diff 与用户确认。

人类可读来源导航见 [`../README.md §5`](../README.md)；机器范围以配置为准。
