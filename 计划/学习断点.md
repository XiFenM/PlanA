# 学习断点（当前光标 · 每次「收尾」覆盖写）

> 开场先读这里——这是 O(1) 续接锚点，让"忘了上次学到哪"归零。
> 本期只带**单一主攻模块**（现为 PyTorch）。历史进度在各模块 `进度.md` 的「每日学习日志」，本文件只保留**当前光标**。
> 维护：「收尾」时由 AI 陪学模式覆盖写「当前断点」并清空「暂停快照」；`/暂停` 时写「暂停快照」。规范见 [陪学流程.md](陪学流程.md)。

## 📍 当前断点

- 更新时间：2026-07-03
- 模块：PyTorch → [../PyTorch/进度.md](../PyTorch/进度.md)
- 资料 / 任务：**文章1 整篇完工 ✅**（Ezyang《PyTorch Internals》读毕，累计 9.0h）。下一份资料：**Ezyang "Let's talk about the PyTorch dispatcher" (2020)**（学习指引 §C #6，预算 2h），配套开**文章2《从 torch.add 追到 kernel》**（对应实践任务 2 + 博客草稿 2，W2 交付物）
- 产出文件：文章1 [../PyTorch/深入学习理解PyTorch/1-Internal-Overview.md](../PyTorch/深入学习理解PyTorch/1-Internal-Overview.md) 完工（仅〔实践〕节留待节假日补：stride 10 题 / 追调用链可与实践任务 1、2 合并做）；文章2 文件待创建（`2-*.md`，按 [../计划/文章模版.md](../计划/文章模版.md) 8 段式起骨架）
- 文章子点：文章2 尚未开篇。开篇动作 = 先猜再学（对 dispatcher 的 key 计算、fallback、boxing 等先写直觉——文章1 §3.2 七步链是现成的"猜"底稿）
- 下一步具体动作：获取 Ezyang dispatcher 博客原文 → 划分小节 → 走先猜再学（重点猜：keyset 怎么从 tensor 算出来、多 key 并存时谁赢、fallback/fallthrough 机制）→ 讲问派盯逐节推进
- cadence：工作日 4.5h（Leetcode 45m + 精读 1.5h + 实战 2h）；当前周次见 [进度总表.md](进度总表.md) 甘特。本模块总预算 ~65h、已记 9.0h
- 欠债 / 后续坑（文章1 §后续预告已挂账，源码走读时顺手核销 ①④）：① Storage 是否已降格为 Tensor 的特殊视图（`untyped_storage()` 线索）；② `__torch_dispatch__` 能力边界（影子对拍 / 轻量布局扩展两个需求）；③ 复数 / 稀疏真扩展源码落点；④ structured kernels（学习指引 §H）

## 🔖 暂停快照（仅 `/暂停` 时有效；`/继续` 后失效，「收尾」清空）

- （空）
