# 学习断点（当前光标 · 每次「收尾」覆盖写）

> 开场先读这里——这是 O(1) 续接锚点，让"忘了上次学到哪"归零。
> 本期只带**单一主攻模块**（现为推理框架）。历史进度在各模块 `进度.md` 的「每日学习日志」，本文件只保留**当前光标**。
> 维护：「收尾」时由 AI 陪学模式覆盖写「当前断点」并清空「暂停快照」；`/暂停` 时写「暂停快照」。规范见 [陪学流程.md](陪学流程.md)。

## 📍 当前断点

- 更新时间：2026-08-09
- 模块：推理框架 → [../推理框架/进度.md](../推理框架/进度.md)
- 断点类型：**资料 / 任务内部**
- 专项：[高级 AI 框架开发工程师 JD 四周冲刺](高级AI框架开发工程师-四周冲刺计划.md) → W1 D1「环境与披露边界 preflight」
- 当前任务：系统梳理固定 vLLM v0.26.0 的整体架构与设计动机。Pass A「仓库地图」与 Pass B「进程和组件边界」及其阶段材料均已完成；当前停在 Pass C-1「OpenAI 请求到 `EngineCoreRequest`」开始前，尚未启动该阶段的用户问答验收。
- 已完成：已把原 PyTorch 阶段边界保存到专项计划 §16.1；固定四个公开源码版本并建立专项工件；完成时间、节奏、环境和披露边界 preflight；完成第一轮“先猜再查”。已确认真实主链包含 `OpenAIServingChat → AsyncLLM → EngineCore（跨进程）→ Scheduler/KVCacheManager → SchedulerOutput → Executor/Worker → ModelRunner → Attention/custom op/backend → device kernel`。已把 vLLM、SGLang、PyTorch 作为浅克隆 submodules 分别固定到 `568afb3a`、`29481685`、`70d99e99`；已将 Pass A–B 整理为阶段文章、原始会话学习记录与 25 张技术记忆卡。宿主机基础指纹详见专项工件；尚未发生可计入进度的学习工时。
- 完成程度：D1 preflight 与本地源码准备已完成。Pass A-1 至 A-3、Pass B-1 至 B-5 均已通过：用户已正确掌握 frontend、独立 DPCoordinator、多个 EngineCore、`uni/mp` Worker/ModelRunner 的进程布局，以及 Worker 与 ModelRunner 的职责边界。`v0.26.0` dense internal DP 文档/源码落差已登记为上游实践候选 U1，暂不定性为 bug、不创建外部 issue。Pass C-1 的导师预核对仍不计为用户已掌握；vLLM/PyTorch 组合运行仍待验证。
- 产出文件：[设计与验证包](../推理框架/EP-PD自研芯片适配设计与验证包.md)；[阶段文章](../推理框架/深入学习理解vLLM/1-Repository-and-Process-Architecture.md)；[学习记录](../推理框架/log/2026-08-09-vllm-repository-and-process-architecture.md)；[技术记忆卡](../推理框架/cards/vllm-repository-and-process-architecture.md)
- 下一步具体动作：启动 Pass C-1，并完成第一道映射题：分别说明 `messages`、`temperature/top_p/max_tokens`、`stream=true + HTTP connection` 在跨入 EngineCore 前被放到哪里或留在哪里，并解释为什么不能把 `ChatCompletionRequest` 原样发给 EngineCore。
- cadence：连续四周执行；2026-08-09 今日主线可用 4h。本专项计划时间只计算主线，Leetcode 与英语均作为额外时间，不计入专项计划时间；实际学习工时在收工时登记。
- 返回点：原 PyTorch 阶段边界已保存在专项计划 §16.1；专项结束后由用户决定是否返回，不自动选择 PyTorch 下一资料。
- 备注：公开时排除个人隐私敏感信息（如手机号），邮箱可公开；同时继续排除公司代码、内部 API、未公开硬件参数、性能数据和原始日志。`mini-ep-pd-serving` 未启动；唯一上游验证锚点按 W1 D5 在 source map 建立后选择。

## 🔖 暂停快照（仅 `/暂停` 时有效；`/继续` 后失效，「收尾」清空）

- （空）
