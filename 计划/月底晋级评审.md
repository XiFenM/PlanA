# 月底晋级评审（已退役）

> **状态**：本入口已于 2026-08-12 退役。保留文件路径，只为让历史周报和旧提交中的链接仍可定位；本文不再是可执行 SOP。

资源晋级现由中央 `resource-planning` 的 review 事务负责，不再以“月底”“本月至少三份周报”或
“五问中三票通过”为前置。review 只处理已通过证据硬门槛、完成冷却并能绑定到精确课程写入槽的
原子 decision unit；排序维度不会替代硬门槛。

当前边界：

- `approved` 只表示用户同意精确编辑集，`applied` 只在目标文件和 registry 事务成功后产生。
- defer、reject、blocked、superseded 不得夹带课程写入。
- 课程 marker 之外的字节与受保护学习状态必须保持不变。
- 历史 W18/W26/W32 的 Top/候选/分数不自动进入当前候选池，也不追加评审结果。
- 每次 review 均需展示精确 diff、事务摘要与 digest，并取得当次确认后才能发布。

静态配置见 [`../.agent-skills-config/resource-planning.json`](../.agent-skills-config/resource-planning.json)，
人类可读导航见 [`../README.md §5`](../README.md)。
