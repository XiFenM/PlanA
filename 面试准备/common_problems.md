# 面试遇到的问题

## 算子优化有哪些常用手段
算子融合（可以重构算法逻辑）
pingpong（传输与计算并行，double buffer ，交换vmem块）
软件流水线，超长指令字（指令展开），分为两阶段，序言（连续发送多个matmul指令）和稳态（边获取matmul结果，一边发送后面的matmul）减少了硬件上的控制电路，硬件调度乱序执行，硬件不做管理。隐藏发送push gain。
mask数据控制流
模板化kernel预处理分支，分支判断提到cpu，借助cpu的控制器能力分支预测
跨越for循环进行指令展开重排

## 如何判断算子、模型优化空间，朝什么方向进行优化？
## 针对不同shape进行特定优化kernel
64合并/128转

## flash attention系列优化
## vllm调度机制
## Weight only int4量化和fp8量化，你觉得二者有什么应用上的区别
## 为什么只量化weight，不量化activation
## 动态量化和静态量化有听说过吗？区别是？
## 什么时候适合动态量化，什么时候适合静态量化
## 写kernel算子时的难点
## 模型运行时的内存碎片问题如何处理（连续内存内存碎片）
## 精度下降时精度问题。如何在模型精度异常时寻找精度异常点？为什么int8、int4、fp8、fp4这种低精度数据类型也可以运行模型产生结果，这些精度比fp32、fp16差了很多，而且matmul中这么多数字相加乘，精度理应已经相差巨大，为什么还能正常产生结果？
## mfu计算
## tp并行 Megatron实现
## cuda编写top k 堆排序
## leetcode线段树
## 推理框架未来发展痛点
## bank conflict
## cuda多线程中线程安全的生产者消费者队列
## 目前工作环境中的问题，期望理想的工作环境是什么样的？（HR面）

