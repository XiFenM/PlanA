您好，我叫 xxx。

我硕士毕业于复旦大学大数据学院，本科毕业于上海大学数学与应用数学专业。目前在一家自研芯片公司担任 AI 框架开发工程师，主要负责国产非 CUDA 架构加速芯片上的大模型推理框架适配、性能优化和模型部署。我的技术栈主要是 `PyTorch` 与 `vLLM`。

我的核心工作可以分成三块。

第一是推理框架和模型适配。我主要围绕 `vLLM` 和 `PyTorch` 做源码级二次开发，参与过 Qwen、 Llama、 DeepSeek、 Ernie 等主流模型，以及 Qwen2.5/3-VL、Ernie4.5-VL等多模态模型在自研芯片上的端到端接入、精度对齐、性能调优和部署支持。这个过程中，我对 `vLLM` 的模型执行链路、调度、KV Cache、Chunked Prefill 等机制比较熟悉，也能从框架层一路定位到算子、通信库和运行时。

第二是性能优化和问题定位。我做过 Conv3d、Matmul、Attention 等热点算子的 Profile、形状分析和协同优化，也结合硬件访存特点推进过 Col-major Matmul 、内存排布调整和算子融合。例如在 Qwen3-VL 链路上，通过与算子同事协同优化 Conv3d，显著降低了显存占用，并将首 Token 延迟下降约 40%；针对 Matmul 占比较大的场景，通过调整权重参数的内存布局提高算子访存效率，使 Llama-3-70B、Qwen-2.5 和 Qwen-3 在少并发场景下的输出吞吐提升约 20%。稳定性方面，我也处理过 `vLLM V1` 异步 H2D/D2D 拷贝导致的低概率精度异常，通过 Replay、Copy/Consumer Trace、回读校验和分层控制变量实验将问题收敛至少数高风险路径，目前根因闭环中。

第三是对外客户与测评接洽。我参与过客户模型部署和第三方测评支持，包括信通院国测、上海 AI Lab、绛溪实验室等外部测评，以及客户侧多模态模型的环境搭建、精度验证、性能分析和问题闭环。在这类工作中，我通常需要把客户或测评侧遇到的问题拆解成可复现 Case，协调模型、算子、通信、运行时、硬件和云服务团队推进修复，并最终交付可部署、可验证的版本。

总体来说，我的优势是既能理解上层模型结构和推理框架，也能下钻到算子、通信库和运行时，把客户部署、第三方测评或线上调试中的问题拆成可复现、可验证、可优化的工程闭环。接下来我希望继续在 LLM 推理框架优化、模型部署和 AI 框架性能优化方向深耕。
