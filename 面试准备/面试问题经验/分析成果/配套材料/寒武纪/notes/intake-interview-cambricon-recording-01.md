# 寒武纪录像接收与分析计划

- 记录 ID：`interview-cambricon-recording-01`。
- 接收与报告日期：2026-09-08；面试实际日期、轮次、岗位 JD 与结果未确认。
- 原件：`work/managed/interview-video-analysis/incoming/寒武纪.mkv`。
- SHA-256：`1006b01c8a1ebaa4935e0376a39b7e6617a60ce78f54276e9cd48469e20ca0b9`。
- 大小：735,462,001 bytes，约 701.39 MiB。
- 容器时长：2201.933 秒，约 36:41.933；AAC 音轨 2201.834 秒，48 kHz 双声道；H.264 1280×720、30 fps。
- 用户请求：了解仓库与既有面试音视频分析流程，随后分析新材料，并明确允许调用 ZenMux API 上传发送材料。授权范围是本场个人复盘；不自动扩展到归档至其他仓库或外部发布。

## 仓库与现有流程

`daily-work` 是日常工作和媒体处理入口。`tools/zenmux`、`tools/media` 提供统一 CLI；`.agent-skills` 与配置层生成本地技能视图；`apps/video` 是单独的 Remotion 工程。面试分析的持久规则、提示词、契约和唯一业务清单在 `projects/managed/interview-video-analysis/`，原始素材与衍生媒体在 `work/managed/interview-video-analysis/`，底稿、审查与独立报告在 `outputs/managed/interview-video-analysis/`。

首场地平线曾使用视频理解、双模型 ASR、校准和技术 QA；文字记录与简记按材料精度直接整理。近期奕行、安霸和进迭时空采用完整音频理解与本地证据审查。历史 Qwen 完整转写存在上游大小/时长拒绝，本次沿用已成功的理解路线，不声明独立 ASR 或完整逐字稿。

基本流程是保留原件、稳定 ID 与哈希登记、媒体检查、制作必要衍生物、绑定精确输入与请求计划、保留原始模型结果、独立整理证据和核查技术、撰写报告、验证清单。产物生成与用户验收分开记录；缺失回答不能写成不会，模型补充知识不能替换现场回答。

## 本地预检和处理

- 已运行仓库 `pnpm media:probe`，保存完整 probe。沙箱阻止 tsx IPC 与 FFprobe，获环境自动审批后运行原仓库命令，未改动工具代码。
- 对完整音轨测量得到 −22.19 LUFS、+0.45 dBTP、LRA 11.90。测量不证明语音可懂度，也不能恢复源录音失真。
- 创建完整双声道 16 kHz、64 kbps MP3；双遍动态 loudnorm 目标 −18 LUFS、−2 dBTP、LRA 11。保留原双声道，不做降噪、静音裁切或内容删节。输出后另测编码文件的实际响度与时长。
- 本地间隔画面抽查出现牛客面试界面；原文件 20:00 画面是默认 A+B 示例，35:00 为视频通话。不能把默认示例归为候选人算法作答，抽样也不证明未抽取区间完全没有代码变化。
- 所有时间以原视频从零开始。会议平台计时器与原文件不同步，不能混用；桌面日期仅作画面观察，不直接认定面试日期。

## 本次外部操作

- Provider / model：ZenMux / `google/gemini-3.7-flash`；已通过实时 `pnpm zenmux models` 确认支持 audio 输入。
- 输入：上述完整衍生音频与本场专用提示词。画面抽查在本地完成，未将帧图片加入该请求。
- 操作：完整音频理解，非流式、`compress=none`、本地上限 50 MiB，一次尝试。
- 费用边界：USD 1.00，`estimate-only`；不是强制货币上限，实际扣费尚需账单确认。输入、模型、提示词、输出及一次尝试构成执行边界。
- 产物：原始 Markdown 底稿与 `.response.json` 元数据，再由本地审查生成独立报告。
- 依据：用户本轮明确允许本场材料通过 ZenMux API 处理。模型与请求参数由 agent 在这项授权内沿用已有成功路线确定，未把先前场次授权复制到本场，也不将此记录表述为用户逐字段审批。
- 调用前运行 dry-run，并通过 Creator Workflow 准备精确摘要；先持久化 dispatching，再执行实际请求。未知结果保留原操作，不自动重发。

## 文档与验收边界

已核对 [ZenMux 官方多模态文档](https://zenmux.ai/docs/zh/guide/advanced/multimodal.html) 和实时模型目录。本地文档缓存不存在；更新脚本会改写 materializer 管理的技能视图，故直接读取官方网页。`pnpm skills:check` 已通过，Creator Workflow context 已按当前配置验证。

使用 Markdown 面试报告契约，不套用地平线岗位校准，也不宣称通过结构化能力 JSON 门禁。技术审查针对底稿中的实际问题查阅公开官方资料，不向公开搜索发送面试原文。音频听辨、角色、专名和时间锚点仍需回听；没有人工或独立 ASR 复核时应明确说明。

主清单与 README 已有进迭时空的未提交成果，本次仅追加寒武纪记录。原先缺失的历史素材不修改登记；本场另外生成从主清单提取的诊断快照，核对新输入与输出。诊断快照不是第二份业务清单。
